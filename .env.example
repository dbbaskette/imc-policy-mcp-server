# Copy this file to .env and fill in your actual values
# The .env file is git-ignored for security

# ============================================================================
# LOCAL DEVELOPMENT CONFIGURATION
# ============================================================================

# OpenAI API Configuration (for local development)
# In Cloud Foundry, these are provided by bound AI services via VCAP_SERVICES
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-5-nano
OPENAI_TEMPERATURE=1.0
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_EMBEDDING_DIMENSIONS=768

# IMPORTANT: gpt-5-nano requires OPENAI_TEMPERATURE=1.0 (does not support custom temperature)
# Embedding dimensions must match your vector store schema (768 for text-embedding-3-small)

# Database Configuration (for local development)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=imc_policy
DB_USER=imc_user
DB_PASSWORD=imc_password

# RAG Configuration
RAG_TOP_K=5
RAG_SIMILARITY_THRESHOLD=0.7
RAG_QUERY_REWRITE=true
RAG_MULTI_QUERY=false
RAG_MULTI_QUERY_COUNT=3

# Vector Store Table Configuration (usually don't need to change these)
VECTORSTORE_TABLE_NAME=vector_store
VECTORSTORE_VECTOR_COLUMN=embedding
VECTORSTORE_CONTENT_COLUMN=content
VECTORSTORE_METADATA_COLUMN=metadata
VECTORSTORE_ID_COLUMN=id

# Application Configuration
# SERVER_PORT=8080

# ============================================================================
# CLOUD FOUNDRY DEPLOYMENT NOTES
# ============================================================================
# When deploying to Cloud Foundry:
# 1. Bind an AI service for chat and embedding models
# 2. Bind a PostgreSQL service for the vector store
# 3. The bound services will automatically provide:
#    - EmbeddingModel bean (overrides local OpenAI configuration)
#    - ChatModel bean (overrides local OpenAI configuration)  
#    - DataSource bean (overrides local database configuration)
# 4. Ensure the bound embedding model dimensions match OPENAI_EMBEDDING_DIMENSIONS
# 5. Set RAG_* environment variables in CF if you want to override defaults